# CLAUDE.md

`windows/install.ps1` is the equivalent of `make configs`: it symlinks the git
config, Claude config (`claude/settings.windows.json`), and the PowerShell
profile (`windows/profile.ps1`). Requires Developer Mode.
